{
  "name": "version",
  "version": "1.0.0",
  "description": "langing page",
  "source": "index.html",
  "scripts": {
    "sass:watch": "sass --style=compressed --watch styles:docs",
    "parcel:watch": "parcel serve --dist-dir public",
    "parcel:build": "parcel build --dist-dir build --public-url ./"
  },
  "author": "RFV",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0",
    "sass": "^1.72.0"
  }
}
